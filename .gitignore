# norns compiles EVERY .sc file under ~/dust into its class library.
# A second definition of any class breaks the build and the device will
# not start -- and a stubbed CroneEngine breaks every engine, not just
# this one. Nothing under test/ may ever be a .sc file.
test/**/*.sc
lib/ugens/build/
*.so
*.scx
